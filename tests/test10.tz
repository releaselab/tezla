parameter nat;
storage nat;
code { DROP;
       PUSH nat 0;
       LEFT nat;
       LOOP_LEFT { DUP; PUSH nat 100; COMPARE; GE;
       IF { PUSH nat 1; ADD; LEFT nat } { RIGHT nat } };
       NIL operation; PAIR };
