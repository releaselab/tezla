parameter unit;
storage (pair (map (pair int (pair nat (pair string (pair bytes (pair mutez (pair bool (pair key_hash (pair timestamp address)))))))) int) (pair (pair (pair (map address nat) (map int nat)) (pair (map nat nat) (map string nat))) (pair (pair (map bytes nat) (map mutez nat)) (map timestamp nat))));
code { DUP; CDR; NIL operation; PAIR; DIP { DROP } }
