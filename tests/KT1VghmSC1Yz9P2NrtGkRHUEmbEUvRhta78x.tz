parameter (or (lambda unit (list operation)) unit);
storage address;
code { DUP; CAR; DIP { CDR }; IF_LEFT { PUSH mutez 0; AMOUNT; COMPARE; EQ; IF {  } { UNIT; FAILWITH }; UNIT; EXEC; PAIR } { DROP; NIL operation; PAIR } }
