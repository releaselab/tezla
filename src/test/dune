(executables
  (names main test)
  (libraries michelson tezla)
  (flags -g)
  (modes byte))

(alias
  (name runtest)
  (action (run ./main.exe ../../../../tests/)))

(alias
  (name test)
  (action (run ./test.exe ../../../../tests/test.tz)))
